# Frontend (SPA)

- Abra `index.html` no navegador para visualizar o layout do workspace.
- O mapa usa OpenLayers via CDN (modo demonstracao).
- Para apontar o frontend para outra API, defina `window.APP_API_BASE` antes de carregar `app.js`.
